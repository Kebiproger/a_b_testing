services:
  gateway:
    image: nginx:alpine
    container_name: gateway
    ports:
      - "${GATEWAY_PORT:-8080}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - ab_testing_net

networks:
  ab_testing_net:
    external: true
